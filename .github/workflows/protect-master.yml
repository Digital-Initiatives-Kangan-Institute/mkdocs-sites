name: Protect Master Branch
on:
  pull_request:
    branches:
      - master

jobs:
  require-dev-to-master:
    runs-on: ubuntu-latest
    steps:
      - name: Check source branch
        if: github.head_ref != 'dev' # Allowed source branch
        run: |
          echo "Error: Only 'dev' branch can merge into 'master'."
          exit 1   